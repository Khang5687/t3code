@AGENTS.md

## Agent skills

### Issue tracker

GitHub issues on the fork `Khang5687/t3code` via `gh`; upstream `pingdotgg/t3code` stays read-only. See `docs/agents/issue-tracker.md`.

### Triage labels

The five default labels, each string equal to its role name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` plus `docs/adr/` at the repo root. See `docs/agents/domain.md`.
